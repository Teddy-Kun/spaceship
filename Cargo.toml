[package]
name = "spaceship"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.12", features = ["derive"] }
rocket = { version = "0.5.1", features = ["json", "secrets", "tls", "uuid"] }
